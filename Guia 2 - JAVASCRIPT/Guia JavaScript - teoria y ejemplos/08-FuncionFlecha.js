// Funcion Flecha. (arrow function)
// misma utilidad que una funcion.
// cambia la sintaxis.
// let nombre = (argumentos si es que lleva) => {cuerpo de la funcion}

let saludar = () => console.log("Buenas!");

saludar();

let suma = (a, b, c) => a + b + c;

console.log(suma(2, 3, 5));
